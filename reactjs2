let style = {
  color: 'white',
  backgroundColor: 'black'
}

const element = (
  <div>
    <h1>Hello!</h1>
    <h2 style={style}>ReactJS</h2>
    </div>
);

ReactDOM.render(
  element,
  document.getElementById('root')
)
